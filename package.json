{
  "name": "app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@material-ui/core": "^4.9.12",
    "@material-ui/icons": "^4.9.1",
    "@okta/okta-react": "1.1.4",
    "@testing-library/jest-dom": "^4.2.4",
    "@testing-library/react": "^9.3.2",
    "@testing-library/user-event": "^7.1.2",
    "axios": "^0.19.2",
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "react-router-dom": "^5.1.2",
    "react-scripts": "3.4.1",
    "react-swipeable-views": "^0.13.9"
  },
  "node": "v13.12.0",
  "npm": "6.14.4",
  "scripts": {
    "start": "cd app && cd http && cd web && cd app && PORT=8080 react-scripts start",
    "build": "cd app && cd http && cd web && cd app && react-scripts build",
    "test": "cd app && cd http && cd web && cd app && react-scripts test",
    "eject": "cd app && cd http && cd web && cd app && react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
