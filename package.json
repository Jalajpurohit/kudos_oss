{
  "name": "app",
  "version": "0.0.1",
  "private": true,
  "engines": {
    "node": "v13.12.0",
    "npm": "6.14.4"
  },
  "devDependencies": {
    "react-scripts": "2.0.5"
  },
  "dependencies": {
    "@material-ui/core": "^3.3.1",
    "@material-ui/icons": "^3.0.1",
    "@okta/okta-react": "^1.1.4",
    "axios": "^0.18.0",
    "react": "^16.6.0",
    "react-dom": "^16.6.0",
    "react-router": "^4.3.1",
    "react-scripts": "2.0.5",
    "react-router-dom": "^4.3.1",
    "react-swipeable-views": "^0.13.0"
  },
  "scripts": {
    "start": "cd app && cd http && cd app && react-scripts start",
    "build": "cd app && cd http && cd app && react-scripts build",
    "test": "cd app && cd http && cd app && react-scripts test",
    "eject": "cd app && cd http && cd app && react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
